<% if model.errors.any? %> #errorが発生したときの条件分岐。modelはパーシャルで渡されたフォームのオブジェクトの変数。（詳細は後述）
    <div class = "alert alert-warning">
        <ul>
            <%= render 'layouts/error_messages', model: f.object %>
                <li><%= message %></li>
            <% end %>
        </ul>
    </div>
<% end %>